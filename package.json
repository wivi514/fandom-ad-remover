{
  "name": "fandom-ad-remover",
  "version": "1.0.0",
  "private": true,
  "description": "Chromium extension that collapses leftover ad containers so blocked ads don't leave empty white boxes.",
  "scripts": {
    "test": "node test/slots.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
